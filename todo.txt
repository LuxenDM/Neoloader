

todo-list items:
	• LME Config tab: make scrollable, then test
	• handle localization of init and recovery_system (only creating the english as a file version, other langs created when nearly done)
	• main.lua initializer and error reporter
	• Setup and Uninstaller modules

further out todo-list items:
	• strict comparison of API surface, verify LME v3.12.x compliancy
	• neomgr v2.1 <- just patch older edition for now, redesign later
		♦ remove bad config values
		♦ open recovery environment (GKProcessCommand recovery)
		♦ move CCD1 generation outside of interface generation?
	• neonotif v1.1 <- verify old edition works as is, or patch
	• expand localization pass to add es, fr, pt languages (maybe also ge, it?)
		♦ recovery: 	lget("access_key|title")
			"lang/en/recovery.ini" [recovery]key=value
		♦ neoloader: 	api.lget(file, header, key)
			"lang/en/<file>.ini" [header]key=value
		♦ fallback: if file doesn't exist, try lang/<file> (copy of english) if locale isn't supported
	• Clean up code formatting for uniformity
	• Create QR code, link to public discord
	• Update config_override.ini
	• Alpha release!
	
Tests required for public beta:
	[ ] Load, execute, manage all existing LME plugins
	[ ] Load all 117 non-LME plugins (no conflicts)
	[ ] Run on fresh install
		[ ] install okay
		[ ] setup env okay
		[ ] add new plugins okay
		[ ] handle bad plugins okay
		[ ] uninstall okay
		[ ] reinstall okay
		[ ] recovery -> nuclear okay
	[ ] run 24 hours: no runaway memory usage?
		[ ] double-check with and without time-based checkpoints
	[ ] exec as new user: doesn't get in way of new player tutorial experience?
	
Delayed for v7.1.0
	• bundled neomgr v3 redesign
	• Expose internal tools via optional bundled plugin
	• Make checkpoint history size configurable
	• Neoloader feature: file tree tracking
		attempts to map directory of game for logging and can 'deep-search' for files
		caches find_file results within caller directory, attempts to speed up file search logically
	• Add 'developer tools' in recovery environment
		♦ rudimentary table browser
		♦ expand log viewer
		♦ metadata browser
	• Change QR code/outbound link in recovery system, link to Neoloader error reporting website
		♦ Website needs to exist first
	• Implement recovery environment report builder
		♦ connects to that website and sends LME error report
	• LME API 3.13.0
		♦ lib.update_state new state_data argument: alias={id = id, ver = version}
			plugins can provide their Class API under an alias ID/Version
		♦ lib.update_state new state_data argument: index=##
			Moves the plugin's index in the game config, shifting others down
		♦ lib.open_utility(util)
			Opens the utility provided by the LME provider, if available
			Planned for Neoloader 7.1.0:
			○ config	: LME management interface
			○ if_config	: Registered Interface Handler
			○ recovery	: Recovery Environment
			○ uninstall	: Launch LME removal utility
			○ about		: LME provider info popup
			
			deprecates lib.open_config(), lib.open_if_config(), and lib.uninstall()
	
